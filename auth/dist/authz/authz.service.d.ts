import { ConfigService } from '@nestjs/config';
import { Driver } from 'neo4j-driver';
import type { components } from '../openapi';
type Role = components['schemas']['Role'];
type RoleCreate = components['schemas']['RoleCreate'];
type RolePatch = components['schemas']['RolePatch'];
type Permission = components['schemas']['Permission'];
type PermissionCreate = components['schemas']['PermissionCreate'];
type PermissionPatch = components['schemas']['PermissionPatch'];
export declare class AuthzService {
    private readonly driver;
    private readonly config;
    constructor(driver: Driver, config: ConfigService);
    private readonly cache;
    invalidateCache(): void;
    authorizeOrThrow(userId: string, roles: string[], method: string, pathname: string): Promise<void>;
    getRolesForUser(userId: string): Promise<string[]>;
    private getCompiledPermissionsForRoles;
    private getCompiledPermsForRole;
    listRoles(): Promise<Role[]>;
    createRole(input: RoleCreate): Promise<Role>;
    getRole(roleId: string): Promise<Role>;
    patchRole(roleId: string, input: RolePatch): Promise<Role>;
    deleteRole(roleId: string): Promise<void>;
    listPermissions(method?: string): Promise<Permission[]>;
    createPermission(input: PermissionCreate): Promise<Permission>;
    getPermission(permissionId: string): Promise<Permission>;
    patchPermission(permissionId: string, input: PermissionPatch): Promise<Permission>;
    deletePermission(permissionId: string): Promise<void>;
    listRolePermissions(roleId: string): Promise<Permission[]>;
    grantPermissionToRole(roleId: string, permissionId: string): Promise<void>;
    revokePermissionFromRole(roleId: string, permissionId: string): Promise<void>;
    listUserRoles(userId: string): Promise<Role[]>;
    assignRoleToUser(userId: string, roleId: string): Promise<void>;
    unassignRoleFromUser(userId: string, roleId: string): Promise<void>;
    private toRole;
    private toPermission;
    private normalizePathPattern;
    private assertCompilablePathPattern;
    private isConstraintError;
    private findPermissionByMethodAndPath;
}
export {};
